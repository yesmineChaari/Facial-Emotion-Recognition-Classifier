project:
  random_seed: 42

data:
  raw_dir: data/raw
  openface_dir: data/openface
  processed_dir: data/processed
  test_size: 0.25

openface:
  executable: FeatureExtraction

training:
  epochs: 30
  batch_size: 32
  learning_rate: 0.001

results:
  dir: results
